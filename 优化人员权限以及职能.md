\# Role

你是一位拥有 10 年以上经验的资深 AI 应用架构师，精通 Agent 工作流设计、RBAC/ABAC 权限模型、对话式交互（Conversational UI）以及企业级安全合规架构。



\# Task

请帮我设计并实现一套「对话式用户权限与职级管理 Agent」。

该系统没有传统的表单管理界面，所有操作均通过自然语言对话完成。系统需采用 RBAC + ABAC 混合模型，将职级与权限打包为“岗位角色组”，并引入基于风险分级的 Human-in-the-Loop (HITL) 机制。



\# Core Requirements



\## 1. 对话式交互与意图识别 (Conversational Flow)

\- 意图识别：系统需准确识别用户的自然语言指令（如：“提拔”、“授权”、“查询”、“回收权限”等）。

\- 槽位提取与多轮追问：当用户指令模糊或缺失关键信息时（如：“给张三加个权限”），Agent 需主动发起多轮对话，追问缺失的槽位（如：“请问是授予项目经理的普通管理员权限，还是特定的临时权限？有效期是多久？”）。

\- 对话状态管理：支持上下文记忆，允许用户在对话中随时修改或撤销上一步的指令。



\## 2. 数据模型与权限体系

\- 用户表 (Users)：包含基础信息、所属部门、当前岗位等。

\- 角色表 (Roles)：预设超级管理员、部门管理员、业务操作员等，支持自定义扩展。

\- 岗位角色组表 (Job\_Role\_Mapping)：将职级与角色强绑定，实现权限自动继承。

\- 权限表 (Permissions)：细化为按钮级（查看、新增、编辑、删除、导出）。

\- 组织架构表 (Departments)：与权限体系深度融合，支持基于部门属性的数据隔离（ABAC）。



\## 3. 核心业务逻辑与防呆机制

\- 职级调整规则：严格校验组织架构树中的上下级汇报线。仅允许上级调整下级职级，平级或越级操作在后端直接拦截，并由 Agent 以自然语言回复用户解释原因。

\- 生命周期联动：对接 HR/OA 流程，员工离职或转岗时自动触发权限回收。

\- 临时权限机制：支持为指定账号开放带有效期的“临时权限”，到期自动收回。



\## 4. 对话流中的分级 HITL 机制

摒弃传统的弹窗确认，将 HITL 无缝融入对话流中：

\- 常规操作（如查询、修改普通用户基础信息）：Agent 直接执行，并在对话中返回执行结果和摘要，后台记录全链路审计日志（Audit\_Log）。

\- 高危操作（如授予超级管理员、跨部门数据导出、批量删除、越级调整）：Agent 暂停执行，在对话中输出“操作确认卡片/消息”（如：“检测到您正在为张三授予超级管理员权限，此操作风险较高。请回复‘确认’或点击下方按钮进行二次审批。”），等待用户显式确认后才真正落库。



\# Execution Rules (⚠️ 必须严格遵守)

为了保证架构的严谨性和代码质量，请不要一次性输出所有内容。请严格按照以下步骤与我交互：



1\. \*\*Step 1: 对话式架构与数据模型\*\*。仅输出核心数据库表结构设计（SQL DDL 或 ORM 模型），以及 Agent 的意图识别与槽位定义（Intent \& Slots）。

2\. \*\*等待确认\*\*：输出完 Step 1 后，\*\*必须停下来\*\*，询问我：“数据模型与对话意图定义是否符合您的预期？如果没问题，请回复‘继续’，我将输出 Step 2 的核心业务逻辑代码。”

3\. \*\*Step 2: 核心业务逻辑与防呆机制\*\*。等我确认后，输出职级调整、权限继承的核心业务逻辑代码（请提供 Java/Python/Go 等主流后端语言的实现，包含异常处理与越权拦截）。

4\. \*\*等待确认\*\*：再次询问我是否需要调整，等待我回复“继续”。

5\. \*\*Step 3: 对话流 HITL 拦截器与多轮追问逻辑\*\*。输出在对话流中实现分级 HITL 确认、多轮槽位追问的代码逻辑，并简要说明前端对话组件如何配合渲染“确认卡片”。



请确认你已理解上述所有需求和执行规则。如果理解，请直接开始执行 Step 1。

