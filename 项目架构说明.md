# 项目架构说明

> 文档路径：`D:\selfFile\python\vue3\v3agent\项目架构说明.md`  
> 编写日期：2026-07-23  
> 适用范围：Vue3 前端、NestJS 网关、FastAPI AI 服务

---

## 一、整体架构定位

本项目由三个独立子工程组成，形成“**用户交互 → 接入网关 → AI 服务**”的三层结构：

```
┌──────────────────────────────────────────────────────────────┐
│  第一层：用户交互层                                            │
│  my-vue-app-ts（Vue3 + Vite + TypeScript）                    │
│  角色：纯浏览器端 Chat UI，负责界面渲染、SSE 消费、本地状态管理     │
├──────────────────────────────────────────────────────────────┤
│  第二层：接入网关层                                             │
│  NestJS（Node.js + Nest）                                      │
│  角色：OpenAI 兼容代理网关，负责模型来源/模型切换、能力下发、         │
│        附件上传、错误统一格式、把请求转发给 AI 服务层               │
├──────────────────────────────────────────────────────────────┤
│  第三层：AI 服务层                                              │
│  ai-service（FastAPI + Python + LangChain/LangGraph）          │
│  角色：真正的 AI 编排引擎，负责模型调用、Agent 工具循环、              │
│        参数加工、会话/RAG 持久化、文档向量化                       │
├──────────────────────────────────────────────────────────────┤
│  外部依赖：模型/向量层                                            │
│  公司 k3 网关、百炼 dashscope、OpenAI 兼容 Embedding 端点         │
└──────────────────────────────────────────────────────────────┘
```

**关键原则**：前端不再执行 LangChain 或任何工具逻辑；它只发 HTTP / 收 SSE。所有 AI 编排、工具调用、密钥管理、持久化都下沉到后端。

**新增**：NestJS 网关已接入 **JWT 账号密码鉴权**，用户数据存储在本地 `users.json`，NestJS 与 ai-service 之间通过 `X-Service-Key` 做双向服务鉴权。前端通过登录界面获取 JWT 后访问所有业务接口。

---

## 二、第一层：Vue3 前端（`my-vue-app-ts`）

### 2.1 角色

- 浏览器内的 Chat SPA（单页应用）。
- 通过 `fetch` 直接调用 NestJS 网关接口。
- 登录后所有请求携带 `Authorization: Bearer <JWT>`。
- 负责 SSE 流式输出、附件上传界面、会话列表 UI、Markdown 渲染。

### 2.2 目录结构

```
my-vue-app-ts/
├── index.html
├── vite.config.ts          # Vite 配置，只挂载 Vue 插件
├── src/
│   ├── main.ts             # 应用入口，渲染 App.vue
│   ├── App.vue             # 根组件，目前只挂载 AgentMode
│   ├── view/
│   │   └── AgentMode/      # 主聊天界面（当前唯一业务视图）
│   │       ├── index.ts    # 组件入口：setup() 中调用 mixin
│   │       ├── index.vue   # 模板 + scoped 样式（600+ 行 UI）
│   │       └── mixin.ts    # 全部业务逻辑：会话、模型、SSE、上传、渲染
│   ├── components/         # 示例组件 HelloWorld.vue（未使用）
│   └── env.d.ts
└── package.json
```

### 2.3 核心模块职责

| 文件 | 角色 | 关键职责 |
|------|------|----------|
| `App.vue` | 应用壳 | 挂载 `AgentMode`，无业务逻辑 |
| `AgentMode/index.vue` | 页面模板 | 侧边栏、会话列表、消息气泡、输入框、弹窗、全部 CSS |
| `AgentMode/index.ts` | 组件入口 | 通过 `useAgentMixin()` 与 `useAuthMixin()` 把逻辑注入模板 |
| `AgentMode/mixin.ts` | 业务逻辑中枢 | 会话管理、模型/来源切换、附件上传、SSE 交互、Markdown 渲染 |
| `AgentMode/auth-mixin.ts` | 登录/鉴权逻辑 | JWT token、登录/注册、apiFetch 自动带 Authorization、401 处理 |
| `AgentMode/auth.ts` | Token 工具 | localStorage 读写 token、解析 JWT payload |

### 2.4 重要状态与方法（来自 `mixin.ts`）

| 分组 | 名称 | 作用 |
|------|------|------|
| 会话 | `conversations`, `activeId`, `activeConv` | localStorage 持久化会话与消息 |
| 模型 | `models`, `currentModel`, `modelCaps` | 从 NestJS 加载模型列表，按能力驱动 UI |
| 来源 | `providers`, `currentProvider` | 切换公司模型 / 百炼模型 |
| 输入 | `userInput`, `attachments` | 文字输入 + 图片/文档附件 |
| 发送 | `handleSend()` | 组装 OpenAI 格式消息，调用 `runAgent()` |
| 流式 | `runAgent()` | `auth.apiFetch` 到 `/v1/chat/completions`，用 `getReader()` 读取 SSE |
| 终止 | `stopAgent()` | `AbortController.abort()` |
| 渲染 | `renderMarkdown()`, `shouldRenderMarkdown()` | markdown-it + highlight.js + 行号 + 复制 |
| 上传 | `uploadFile()`, `addAttachment()` | multipart 上传到 NestJS `/v1/files/upload` |
| 登录 | `submitAuth()` / `logout()` | 调用 `/auth/login` 或 `/auth/register`，JWT 存 localStorage |

### 2.5 前端往后端发送的数据

```json
{
  "model": "k3",
  "messages": [
    { "role": "user", "content": "..." },
    { "role": "user", "content": [{ "type": "text", "text": "..." }, { "type": "image_url", "image_url": { "url": "data:image/..." } }] }
  ],
  "stream": true,
  "enable_thinking": true
}
```

---

## 三、第二层：NestJS 网关（`NestJS`）

### 3.1 角色

- **OpenAI 兼容代理**：前端把 `baseURL` 指向 `http://localhost:3000`。
- **JWT 账号密码鉴权**：新增 `users.json` 本地存储用户，注册/登录后签发 JWT。
- **全局路由保护**：`/`、`/auth/*` 公开，其余路由需 `Authorization: Bearer <token>`。
- **路由/转发层**：只负责选择模型来源、查询模型列表、附件上传，不执行 AI 编排。
- **安全隔离**：模型 API Key 只在后端环境变量里，不暴露给浏览器。

### 3.2 目录结构

```
NestJS/
├── .env                  # 模型来源、密钥、端口、AI_SERVICE_URL、JWT、service key
├── .env.example
├── package.json
├── users.json            # 用户账号密码哈希（bcryptjs），自动创建
├── src/
│   ├── main.ts           # 启动入口：关闭默认 bodyParser，开启 CORS，监听 PORT
│   ├── app.module.ts     # 根模块：注册全局 JwtAuthGuard
│   ├── app.controller.ts # 根路由 /，公开
│   ├── app.service.ts    # 根服务
│   ├── auth/             # JWT 鉴权模块
│   │   ├── auth.module.ts
│   │   ├── auth.controller.ts # POST /auth/register、/auth/login
│   │   ├── auth.service.ts    # 注册、登录、签发 JWT
│   │   ├── jwt.strategy.ts
│   │   ├── jwt-auth.guard.ts
│   │   └── public.decorator.ts
│   ├── users/            # 用户管理模块
│   │   ├── users.module.ts
│   │   ├── users.controller.ts  # GET /v1/users/me
│   │   ├── users.service.ts     # 用户 CRUD、bcrypt 密码
│   │   └── users.store.ts       # JSON 文件持久化
│   ├── chat/
│   │   ├── chat.module.ts
│   │   ├── chat.controller.ts   # 路由：/v1/providers、/v1/models、/v1/chat/completions（JWT 保护）
│   │   └── chat.service.ts      # 多来源管理、能力配置、转发到 FastAPI
│   └── files/
│       ├── files.module.ts
│       ├── files.controller.ts  # 路由：/v1/files/upload、/v1/files/delete（JWT 保护）
│       └── files.service.ts     # 上传、pdf/docx/txt 提取、定时清理
└── uploads/              # 上传文件落盘目录
```

### 3.3 核心模块职责

| 文件 | 角色 | 关键职责 |
|------|------|----------|
| `app.module.ts` | 根模块 | 组合 AuthModule、UsersModule、ChatModule、FilesModule；注册全局 JWT Guard |
| `main.ts` | 入口 | 监听 `PORT`（默认 3000），允许大 body（50mb），开启跨域，全局 ValidationPipe |
| `auth.controller.ts` | 鉴权路由 | `POST /auth/register`、`POST /auth/login` |
| `auth.service.ts` | 鉴权服务 | bcryptjs 密码哈希、JWT 签发 |
| `jwt-auth.guard.ts` | 全局守卫 | 默认保护所有路由，`@Public()` 可放行 |
| `users.service.ts` | 用户服务 | `users.json` 文件持久化、用户查找与创建 |
| `chat.controller.ts` | 聊天路由 | 受 JWT 保护，从 JWT 取 userId，转发到 FastAPI |
| `chat.service.ts` | 聊天服务 | 管理多来源、转发时带 `X-Service-Key` + `X-User-Id` |
| `files.controller.ts` | 文件路由 | 接收 multipart 上传，批量删除（JWT 保护） |
| `files.service.ts` | 文件服务 | 提取 pdf/docx/txt 文本、保存到 `./uploads`、定时清理 3 天前文件 |

### 3.4 关键路由与流程

| 路由 | 方法 | 说明 |
|------|------|------|
| `/` | GET | 公开，健康提示 `Hello World!` |
| `/auth/register` | POST | 公开，用户名密码注册 |
| `/auth/login` | POST | 公开，用户名密码登录，返回 JWT |
| `/v1/users/me` | GET | 受保护，返回当前用户信息 |
| `/v1/providers` | GET | 受保护，返回可用来源和当前来源 |
| `/v1/providers/current` | PUT | 受保护，切换模型来源 |
| `/v1/models` | GET | 受保护，返回当前来源的模型列表 + 能力标记 |
| `/v1/models/current` | PUT | 受保护，切换当前模型 |
| `/v1/chat/completions` | POST | 受保护，强制 `model` 为当前来源默认模型，转发给 FastAPI |
| `/v1/files/upload` | POST | 受保护，接收 `files` 多文件，返回 `{ id, name, kind, text }` |
| `/v1/files/delete` | POST | 受保护，按 `ids` 批量删除文件 |

### 3.5 转发给 FastAPI 的关键头

```
Content-Type: application/json
X-Provider: company | bailian
X-User-Id: <前端会话ID，临时作为用户标识>
```

---

## 四、第三层：FastAPI AI 服务（`ai-service`）

### 4.1 角色

- **AI 编排引擎**：真正调用模型、执行工具循环、加工请求参数。
- **OpenAI 兼容端点**：提供 `/v1/chat/completions`，对前端/NestJS 透明。
- **持久化与 RAG**：会话/消息存 SQLite，文档分块后向量化入库，检索时注入上下文。

### 4.2 目录结构

```
ai-service/
├── .env                  # 模型网关地址、密钥、端口、数据库、上传目录、Embedding 配置
├── requirements.txt      # FastAPI / uvicorn / httpx / langchain / sqlalchemy 等
├── ai_service.db         # SQLite 数据库（会话、消息、文档、chunks）
├── uploads/              # 文档上传后保存原文件的目录
└── app/
    ├── main.py           # FastAPI 入口：路由、生命周期、RAG 注入、转发/Agent 路径
    ├── agent.py          # LangChain/LangGraph Agent 编排、工具、SSE、参数加工
    ├── db.py             # SQLAlchemy 模型：Conversation / Message / Document / Chunk
    └── rag.py            # 文档分块、Embedding、向量相似度检索
```

### 4.3 核心模块职责

| 文件 | 角色 | 关键职责 |
|------|------|----------|
| `main.py` | 入口与路由 | 启动 `FastAPI`，定义 `/health`、`/v1/chat/completions`、会话 API、文档 API、RAG 搜索 API |
| `agent.py` | AI 编排 | 构建 `ChatOpenAI` / ReAct Agent、时间注入、联网搜索/思考模式处理、SSE 输出 |
| `db.py` | 数据模型 | `Conversation`、`Message` 的 CRUD，SQLite 起步，可换 PostgreSQL |
| `rag.py` | RAG 检索 | `chunk_text()` 分块、`embed_texts()` 向量化、cosine 相似度检索 |

### 4.4 关键路由

| 路由 | 方法 | 说明 |
|------|------|------|
| `/health` | GET | 返回服务状态和可用来源 |
| `/v1/chat/completions` | POST | 核心接口：无 tools 直接调用模型，有 tools 走 ReAct Agent |
| `/v1/conversations` | GET/POST | 列出/创建会话 |
| `/v1/conversations/{id}` | GET/DELETE | 获取/删除会话 |
| `/v1/conversations/{id}/messages` | POST | 追加一条消息 |
| `/v1/documents` | POST | 上传文档 → 分块 → Embedding → 入库 |
| `/v1/rag/search` | POST | 向量检索 Top-K chunks |

### 4.5 请求处理分支（`/v1/chat/completions` 内部）

```
收到请求
    │
    ├── 提取 X-Provider, X-User-Id
    │
    ├── RAG 注入：用最后一条用户问题检索 Top-K chunks，拼成 system 消息
    │
    ├── preprocess()：注入时间、联网搜索、剔除不支持的思考模式参数
    │
    ├── 是否带 tools？
    │       ├── 是 → run_agent() → ReAct Agent 循环（服务端执行工具）
    │       └── 否 → forward_upstream() → 直接调用模型
    │
    └── 返回 OpenAI 兼容 JSON 或 SSE 流
```

### 4.6 工具示例（`agent.py`）

```python
@tool
def get_weather(city: str) -> str:
    """当用户询问某个城市的天气时，调用此工具获取实时天气信息"""
    return f"{city} 今天晴朗，气温 25℃，微风。"
```

工具在服务端执行，前端只收到 `tool_start` / `tool_end` 事件提示。

---

## 五、三层交互流程

### 5.1 普通对话流式请求

```
用户输入 → Vue3
  │
  ├─ POST http://localhost:3000/v1/chat/completions
  │   NestJS 强制 model 为当前来源模型，添加 X-Provider / X-User-Id
  │
  ├─ POST http://localhost:8000/v1/chat/completions
  │   FastAPI 做 RAG 注入、时间注入、参数加工
  │
  ├─ 调用外部模型网关（公司 k3 / 百炼）
  │
  └─ SSE 流原路返回给 Vue3
```

### 5.2 带工具（天气）的对话

```
Vue3 → NestJS → FastAPI
  │
  FastAPI 创建 ReAct Agent
  │
  ├── 模型决策：需要调用 get_weather
  ├── 发送 tool_start 事件给前端
  ├── 执行工具（服务端）
  ├── 发送 tool_end 事件给前端
  └── 模型再次生成最终回答
  │
  SSE 返回 token + done
```

### 5.3 文档上传与 RAG

```
用户上传文档 → Vue3
  │
  ├─ POST /v1/files/upload 到 NestJS
  │   NestJS 提取文本，返回前端
  │
  ├─ 前端把文档文本随用户消息一起发（当前实现）
  │
  └─ 后续改进：POST /v1/documents 到 FastAPI
      FastAPI 分块 → Embedding → 存 SQLite
      对话时自动 RAG 检索注入上下文
```

---

## 六、数据流与关键配置

### 6.1 环境变量

#### `NestJS/.env`

| 变量 | 含义 |
|------|------|
| `MODEL_BASE_URL` | 公司网关地址（不含 `/chat/completions`） |
| `MODEL_API_KEY` | 公司网关密钥 |
| `MODEL_NAME` | 默认模型，如 `k3` |
| `BAILIAN_MODEL_*` | 百炼 dashscope 配置 |
| `VISION_MODELS` | 支持图片的模型名称匹配 |
| `DOC_MODELS` | 支持文档的模型名称匹配 |
| `THINKING_MODELS` | 支持思考模式的模型名称匹配 |
| `PORT` | 网关端口，默认 3000 |
| `AI_SERVICE_URL` | FastAPI 地址，默认 `http://localhost:8000` |

#### `ai-service/.env`

| 变量 | 含义 |
|------|------|
| `MODEL_BASE_URL` / `MODEL_API_KEY` | 公司模型网关 |
| `BAILIAN_MODEL_*` | 百炼配置 |
| `THINKING_MODELS` | 思考模式名称匹配 |
| `PORT` | FastAPI 端口，默认 8000 |
| `DATABASE_URL` | 数据库，如 `sqlite+aiosqlite:///./ai_service.db` |
| `UPLOAD_DIR` | 文档上传目录 |
| `EMBED_MODEL` / `EMBED_PROVIDER` | Embedding 模型与来源 |

### 6.2 端口对照

| 服务 | 开发端口 | 入口文件 |
|------|----------|----------|
| Vue3 前端 | 5173 | `my-vue-app-ts/src/main.ts` |
| NestJS 网关 | 3000 | `NestJS/src/main.ts` |
| FastAPI AI 服务 | 8000 | `ai-service/app/main.py` |

---

## 七、当前能力与待完善

### 7.1 已具备

- Vue3 聊天界面 + SSE 流式 + Markdown 渲染 + 附件上传。
- NestJS 多模型来源管理 + OpenAI 兼容转发 + 能力下发。
- FastAPI 参数加工 + ReAct Agent + 工具服务端执行 + 会话持久化 + RAG 管线（初版）。

### 7.2 待完善

| 方向 | 说明 |
|------|------|
| 用户认证 | 目前 `X-User-Id` 由前端临时会话 ID 充当，需引入 JWT / OAuth |
| 前端对接持久化 | 会话仍存 localStorage，需改为调用 `/v1/conversations` |
| 向量数据库 | 当前 SQLite 线性扫描，生产应换 pgvector / Milvus |
| 生产部署 | 缺 Docker / docker-compose 一键启动 |
| 可观测性 | 缺日志链路追踪、token 用量统计 |

---

## 八、快速启动

```bash
# 1. FastAPI AI 服务（端口 8000）
cd ai-service
.venv/Scripts/python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

# 2. NestJS 网关（端口 3000）
cd NestJS
npm run start:dev

# 3. Vue3 前端（端口 5173）
cd my-vue-app-ts
npm run dev
```

启动后访问：http://localhost:5173
