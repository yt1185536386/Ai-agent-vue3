# NestJS 网关 × Java model-gateway 架构分析报告

> 分析范围:`packages/NestJS`(业务网关,6011)、`packages/model-gateway`(模型出口网关,6015)
> 日期:2026-09-10
>
> 三个问题:① 鉴权拆分是否合理、有无重复冗余;② 功能拆解是否各司其职、是否符合企业架构;
> ③ 企业视角还需要哪些功能扩展与检测预埋点。

---

## 〇、现状链路(分析基准)

```
浏览器 ──JWT──> NestJS(6011)──X-Service-Key──> ai-service(6010)──X-Service-Key──> Java 网关(6015)──channel key──> 上游模型
                 │JWT签发/校验                    │透传身份                        │只校验JWT不签发
                 │权限规则引擎(唯一写规则)          │                               │限流/熔断/计量/渠道/告警
```

两侧鉴权入口:
- NestJS:全局 `JwtAuthGuard`(支持 @Public、内部服务 X-Service-Key + X-User-Id 模拟身份)→ 全局 `PermissionGuard`(每请求查库加载用户 + 解析有效权限码)
- Java:`AuthInterceptor`(/api/**,校验 NestJS 签发的 JWT,共享密钥) + `ServiceKeyInterceptor`(/v1/**,服务密钥 + 身份透传)

---

## 一、鉴权拆分分析

### 1.1 拆分方向:✅ 合理

**"签发与校验分离"本身是正确的企业级设计**:

| 设计点 | 评价 |
|---|---|
| JWT 只在 NestJS 签发,Java 只校验不签发 | ✅ 单一签发方,密钥面小,职责清晰 |
| Java 以 DB 为准重新加载用户(禁用/权限变更即时生效) | ✅ 比信任 JWT claim 更安全,避免"改了权限还要等 token 过期" |
| 内部调用统一 X-Service-Key,用户身份仅作计量/限流维度 | ✅ 内部信任边界明确,模型出口不做用户权限语义 |
| Java 错误体区分 /api( ApiResponse)与 /v1( OpenAI 兼容 error 结构) | ✅ 细节到位 |

### 1.2 重复与冗余(重点)

#### 🔴 R1|权限解析规则两份实现,靠注释约定同步

`NestJS/src/users/users.service.ts#resolvePermissionCodes` 与
`model-gateway/.../perm/PermissionService#resolveCodes` 是**同一套规则的 TS / Java 双实现**:
超管全通 → 个人绑定(allowed 覆盖)→ 职级继承 → 默认拒绝。代码注释互相引用"与 NestJS 同一规则"。

这是最大的冗余风险:规则演进(如增加部门维度、有效期、审批流)时必须**人肉同步两处**,
漏一处就是静默的权限口径分叉——企业里这类"双权威"是安全审计的高危项。

**建议(按成本递增)**:
1. 短期:以 NestJS 为唯一权威,Java 不再解析权限码,改为调用 NestJS 内部接口
   `/internal/users/{id}/effective-permissions`(X-Service-Key 鉴权)+ 本地缓存 30~60s + 变更时主动失效。
2. 中期:权限快照写入共享存储(Redis 或 MySQL 一张 `user_effective_perms` 物化表),NestJS 写、Java 读,
   读路径零跨服务调用。
3. 长期:若服务数继续增长,权限点收编到独立 IAM/鉴权中心(JWT 内嵌 scope 或 JWKS + 短时效 token)。

#### 🟠 R2|鉴权语义的"隐形重复"

以下逻辑在两处各写一遍,且全靠注释对齐:
- `super` / `auth` 两个特殊值的语义(Java 的 `RequirePermission` 常量 vs NestJS 的 `PERM_SUPER`/`PERM_AUTH`)
- 账号禁用检查(`status !== 1` 两处)
- 权限码种子表(`PERMISSION_SEEDS` 在 NestJS,Java 靠共享同一张 permissions 表 + DataInitializer)

特殊值与种子表属于"协议",建议抽到共享的 schema/常量定义(JSON 契约文件或共享 migration),两侧消费,消灭口头约定。

#### 🟠 R3|JWT claims 签发了但没人用

`AuthService.signToken` 塞了 `isSuperAdmin / jobLevelRank / jobLevelName`,而两侧鉴权都是"DB 为准",
**这些 claim 实际不参与任何决策**。冗余 claim 的危害:读者会误以为改 claim 即可提权(误导),
且用户提级后 token 里的旧职级与新权限并存,排查问题时有干扰。
**建议:要么删掉,要么升级为"claim 快照 + 变更刷新"机制**;至少在 JWT 里补 `jti` 和 `iat`,为后续吊销留位。

#### 🟠 R4|每请求多次查库 ×2,无任何缓存

- NestJS `PermissionGuard`:findById + resolvePermissionCodes(权限点全表 + 个人绑定 + 职级绑定)≈ **4 次/请求**
- Java `AuthInterceptor`:同样约 4 次查库/请求

管理端每个按钮请求都打 4 次库。企业并发下这是第一个瓶颈。
**建议:用户+权限快照缓存 30~60s(Caffeine / NestJS 内存或 Redis),权限写操作时精确失效**——
代码里权限变更都收敛在 NestJS 权限维护接口,失效点天然集中,做主动失效成本很低。

#### 🟡 R5|服务密钥体系单薄

- 同一把 `NESTJS_SERVICE_KEY` 用于 NestJS↔ai-service 与 ai-service↔Java(以及 NestJS→Java /models),
  **一把钥匙开三扇门**,无按调用方区分、无轮换机制、无过期时间
- 密钥比较用 `===` / `equals`(非常量时间比较,理论侧信道;内网风险低但企业安全扫描会报)
- Java `ServiceKeyInterceptor` 完全信任 `X-User-Id` 头——持有密钥即可冒充任意用户计量(可接受,但应有意识)

**建议:每对服务独立密钥 + 支持双密钥并行(灰度轮换)+ 常量时间比较(`crypto.timingSafeEqual` / `MessageDigest.isEqual`)。
更大规模再考虑 mTLS 或内部 OIDC(client credentials)。**

### 1.3 鉴权层的小问题

| 位置 | 问题 |
|---|---|
| NestJS `main.ts` / Java `WebConfig` | CORS `origin: true / allowedOriginPatterns("*")` 且 `allowCredentials(true)`——等于全站放开带凭证跨域,企业安全扫描必挂。应改为白名单列表 |
| Java `AuthInterceptor` | 拦 `/api/**` 但**无 @RequirePermission 注解的接口直接放行**;靠每个 Controller 记得加 `AuthContext.require()`。默认开放 vs NestJS 全局默认需要登录,两者哲学不一致,Java 侧存在"忘加注解 = 裸奔"的隐患。建议改为默认 401 + @Public 显式放行 |
| NestJS | 登录接口无失败次数限制/验证码,可被撞库;无 refresh token 机制,access token 7 天且**无登出吊销手段**(无 jti/黑名单) |
| 两侧 | JWT 密钥不足 32 位时 Java 循环填充、NestJS 直接用——同一 secret 不同补齐规则理论上有分叉风险(实际约定强密钥即可,但值得写进配置校验) |

---

## 二、功能拆解分析

### 2.1 各司其职:✅ 大方向正确,且有几个亮点

| 职责 | 归属 | 评价 |
|---|---|---|
| 用户/权限**写**规则引擎 | NestJS 唯一 | ✅ 正确,业务规则只写一次 |
| JWT 签发 | NestJS 唯一 | ✅ |
| 限流 / 熔断 / TPM / 计量(invoke_logs) | Java 唯一 | ✅ 计量单一来源,避免双份对账 |
| 渠道管理与上游密钥 | Java 唯一(channels 表) | ✅ 真实 key 只在一处 |
| 模型能力元数据(vision/doc/thinking) | NestJS 环境变量 | ⚠️ 见 D2 |
| 告警评估(失败率/时延/限流命中) | Java | ✅ 贴近数据源,合理 |
| 业务审计 | NestJS | ✅ 贴近业务写操作,合理 |

这个形态就是企业里经典的 **BFF(NestJS)+ 资源网关(Java)** 模式,方向符合业界实践
(对照:轻量业务网关负责会话与编排,重型 API 网关负责流量治理)。

### 2.2 功能拆解的问题

#### 🔴 D1|双向跨库读表:分布式单体的味道

- NestJS 直接读 Java 拥有的 `channels` 表(TypeORM 只读映射,15s 缓存)
- Java 直接读 NestJS 拥有的 `users / permissions / user_permissions / job_level_permissions` 表

两边**各自声明了一份对方表的实体**,schema 演进时互不知情(改表名/字段类型不编译报错,运行时才炸)。
这是比 R1 更根本的耦合:服务拆了,数据库没拆。

**建议**:不必一步到位拆库(单体部署期共享库可接受),但要把"表所有权"制度化:
1. 每张表标注 owner;非 owner 一律 `synchronize: false` + 只读视图(现状已部分做到,值得肯定);
2. 跨服务取数逐步换成内部 API + 缓存(NestJS 取渠道走 Java 的 `/api/channels`;Java 取用户走 NestJS internal 接口), DB 直连作为降级;
3. 若未来上多实例/分库,这是第一改造点。

#### 🟠 D2|模型能力元数据散落在 NestJS 环境变量

`VISION_MODELS / DOC_MODELS / THINKING_MODELS` 靠模型名子串匹配,配置在 NestJS。
模型能力本质是**渠道/模型的属性**,归属应该在 Java 的 channels(加 `capabilities` 字段),
NestJS 只透传。否则新增渠道时要多改一个服务的环境变量,容易漂移。

#### 🟠 D3|限流 / 熔断 / TPM 全部进程内存态

`RateLimiter`(ConcurrentHashMap 令牌桶)、`CircuitBreaker`(内存滑窗)都是单 JVM 内存态:
- **水平扩展即失效**(多实例配额 = N 倍),熔断状态各实例不一致
- 重启丢失计数,发布瞬间配额重置

学习项目可接受;企业化第一步是 **Redis 化**(令牌桶用 Lua 原子脚本,熔断状态共享),或直接上
Sentinel / Resilience4j + 集中式存储。

#### 🟠 D4|转发链路 4 跳,SSE 三段透传无保护

浏览器 → NestJS → FastAPI → Java → 上游。延迟叠加 + 三处 SSE 透传,任一跳挂掉都表现为前端"流断了"。
链路长本身是职责拆分的代价(可接受),但缺配套:
- 全链路无 request-id 贯穿(见第三节 P1),排障只能靠时间戳对齐
- NestJS 转发用裸 fetch,无超时/重试预算/连接池参数;Java 非流式 RestClient 未配连接池(`SimpleClientHttpRequestFactory` 默认无池化)
- 各跳的 502/504 错误文案会层层拼接(前端会看到嵌套括号的复合错误),建议统一错误码 + 每跳只追加自己的维度

#### 🟡 D5|其他

- NestJS `TypeORM synchronize: true`——开发方便,生产会自动改表,企业环境必须关掉换 migration
- 请求体 50MB 上限(base64 图片),缺分片/直传对象存储方案,大附件走网关链路浪费
- Java `AuthController` 的 `/api/auth/me` 与 NestJS `/v1/auth/me` 职能重叠(一个查库脱敏),前端两边都调?建议明确"用户态只在 NestJS"

### 2.3 企业架构符合度结论

**当前是"教科书式的学习架构":边界划分的理念全对,但缺少企业化的加固层**——
权限双实现(R1)、双向跨库(D1)、内存态限流(D3)是三个最需要在团队扩大/流量上来前解决的结构性风险。
其余(错误码、超时池化、CORS、migration)属于企业上线 checklist 项。

---

## 三、企业级功能扩展与检测预埋点(重点)

### 3.1 可观测性(最优先,现在几乎没有)

| # | 预埋点 | 落点 | 说明 |
|---|---|---|---|
| P1 | **全链路 Request-Id / Trace-Id** | NestJS 中间件生成 `X-Request-Id`,随 X-头透传给 ai-service 与 Java;Java 拦截器入 MDC,落进 invoke_logs;响应头回传前端 | 排障从"对时间戳"变成"一个 id 查到底",**成本最低收益最大的一项**。后续接 OpenTelemetry(W3C traceparent)可平滑升级 |
| P2 | **Metrics 指标暴露** | Java:Spring Boot Actuator + `/actuator/prometheus`(QPS、TPM、渠道失败率、熔断状态、令牌桶余量、TPM 预扣/结算差值);NestJS:prom-client 暴露进程指标 + 业务计数器 | 告警模块现在是自研 DB 轮询,应保留 DB 告警、增加 Prometheus 标准输出,企业监控栈(Grafana)直接可接 |
| P3 | **首 token 延迟(TTFT)** | Java 流式转发处:上游响应头到达时间 − 请求开始时间,写入 invoke_logs 新列 | LLM 网关最核心的用户体验指标;现在只有总耗时 |
| P4 | **结构化日志** | 两侧统一 JSON 日志(requestId、userId、channel、model、latency、errorCode),NestJS 目前是纯文本 Logger | 为接 ELK/Loki 预埋 |
| P5 | **健康检查** | 两侧 `/health`(liveness)+ `/health/ready`(依赖检查:DB、上下游连通);启动脚本(start-all.ps1)改为按健康检查拉起 | 现在靠端口探活,无法发现"活着但废了"的状态 |

### 3.2 安全与合规

| # | 扩展点 | 说明 |
|---|---|---|
| S1 | **Token 吊销与登出** | JWT 加 `jti`,Redis 黑名单或短 access + refresh token 双 token;否则"退出登录/踢人"做不到(现在改密码也踢不掉已签发的 7 天 token) |
| S2 | **登录防爆破** | NestJS 登录接口失败计数 + 锁定/验证码 + IP 限流 |
| S3 | **密钥轮换** | 内部 service key 按"调用方对"独立 + 双密钥灰度;JWT secret 支持 kid 版本化 |
| S4 | **审计扩展** | NestJS 审计只覆盖业务写操作;补:登录/登出事件、权限变更前后快照(diff)、Java 侧 /api 管理操作(渠道增删改、策略变更)应回传 NestJS 审计或独立审计流 |
| S5 | **CORS/响应头加固** | 白名单 origin;Helmet / 安全响应头 |
| S6 | **内容合规与脱敏埋点** | ai-service/Java 侧预埋敏感词与 PI 过滤钩子位(企业 LLM 出口的合规刚需),先留 hook 不启用 |

### 3.3 容量、配额与成本(模型网关的企业刚需)

| # | 扩展点 | 说明 |
|---|---|---|
| C1 | **部门/用户配额层级** | 现在 TPM/QPM 按策略+目标 key;企业需要 部门日/月 token 预算 → 用户配额继承,超限降级或拒绝。埋点:配额检查挂在 Java `reserveTpm` 之前 |
| C2 | **成本归集** | invoke_logs 增加单价/成本列(渠道配置模型单价),计量即计费;看板按部门/用户/模型出成本报表 |
| C3 | **多租户维度** | CurrentUser 增加 departmentId/tenantId 维度(从透传头获得),限流与计量都按租户分桶 |
| C4 | **降级与排队** | TPM 不足时现在直接 429;企业做法:进队列短暂等待 + 低优先级请求降级到备用渠道 |
| C5 | **语义缓存(预埋)** | 相同/相似问题命中缓存的 hook 位(命中需谨慎,先只做指标统计不做拦截) |

### 3.4 高可用与治理

| # | 扩展点 | 说明 |
|---|---|---|
| H1 | **限流/熔断 Redis 化** | 见 D3,多实例前置条件 |
| H2 | **渠道健康探测** | Java 定时轻量探活各渠道(/models 或最小 completion),结果入 channels 状态;调度时结合熔断 + 探活双信号 |
| H3 | **渠道灰度/权重路由** | channels 有 priority;预埋 weight 字段,同一模型多渠道按权重分流 + 灰度新渠道 |
| H4 | **重试与超时预算** | Java 对幂等的上游错误(5xx/超时)按渠道重试 1 次;各跳统一超时预算(如上游 120s,NestJS→ai 130s…)避免前面先超时 |
| H5 | **优雅停机** | 两侧 graceful shutdown(SSE 流进行中不再接新请求,存量流排空) |
| H6 | **配置中心化** | .env 散落各包,企业化用统一配置中心或至少集中 secrets 管理(数据库密码、上游 key 不进 git) |

### 3.5 实施优先级建议

1. **P1 全链路 Request-Id**(1 天级,收益/成本比最高)
2. **R4 权限快照缓存** + **S2 登录防爆破**(各 1 天,削掉最大的性能/安全两个裸露点)
3. **P5 健康检查 + H5 优雅停机**(部署可靠性)
4. **R1 权限解析收敛到 NestJS 单权威**(2~3 天,消灭双实现)
5. **P2/P3 Metrics + TTFT**(可观测性成型)
6. **C1/C2 配额与成本**(企业计费刚需)
7. **H1 Redis 化限流熔断**(多实例前必须)
8. **D1 表所有权治理 / S1 吊销机制**(架构演进期解决)

---

## 附:核对过的事实基础

- NestJS 全局守卫链:JwtAuthGuard(@Public / 服务密钥模拟)→ PermissionGuard(每请求查库)
- Java 拦截器:AuthInterceptor(/api/**,JWT+DB+权限码)、ServiceKeyInterceptor(/v1/**,密钥+身份透传)
- 权限解析双实现:`users.service.ts#resolvePermissionCodes` ≡ `PermissionService#resolveCodes`(规则一致,人工同步)
- 计量唯一来源:invoke_logs(QPM/TPM 两段式结算、渠道调度跳熔断、流式收尾取 usage)
- 告警:AlertEvaluator 15s 轮询(熔断事件/失败率/时延/限流命中,30 天保留)
- 跨库:models.service.ts 只读 channels 表;model-gateway 读 users/perm 四表
- RateLimiter/CircuitBreaker 均为进程内存实现(ConcurrentHashMap)

---

## 已实施加固(2026-09-10)

### NestJS 网关(构建 ✅)

| 项 | 文件 | 内容 |
|---|---|---|
| 全链路 Request-Id | 新增 `src/common/request-id.middleware.ts`;`main.ts` 注册;`chat.service/controller` 透传 `X-Request-Id` 给 ai-service | 生成/沿用请求 ID,写响应头,随转发链路下发 |
| 权限快照缓存 | `users.service.ts` 新增 `loadAuthContext()`(30s TTL)+ `invalidateAuthCache()`;`permission.guard.ts` 改用缓存 | 每请求 ~4 次查库 → 命中缓存;权限绑定/禁用/删除用户后主动失效,语义不变 |
| 登录防爆破 | `auth.service.ts` | 同 username+ip 连续失败 5 次锁定 15 分钟 |
| JWT 修正 | `auth.service.ts` | 加 `jti`(为吊销预留);删除两侧都不用的 `isSuperAdmin/jobLevelRank/jobLevelName` claims |
| 常量时间比较 | `jwt-auth.guard.ts` | 服务密钥改 `crypto.timingSafeEqual` |
| CORS 白名单 | `main.ts` | `origin:true` → 白名单(默认本地 6012/6013,`CORS_ORIGINS` 环境变量覆盖) |
| 健康检查 | `app.controller.ts` | `GET /health`:DB 探测,不通返回 503 |
| 优雅停机 | `main.ts` | `enableShutdownHooks()` |
| 表结构同步显式化 | `app.module.ts` | `synchronize` 由 `DB_SYNCHRONIZE` 控制(默认 true 保开发习惯,生产必须 false) |

### Java model-gateway(编译 ✅)

| 项 | 文件 | 内容 |
|---|---|---|
| 全链路 Request-Id | 新增 `common/RequestIdFilter.java`(MDC + 响应头);`InvokeLogEntity` 加 `requestId` 列;`InvokeService#newLogRow` 落值;日志 pattern 内嵌 requestId | 排障一个 id 贯穿 NestJS→ai-service→model-gateway 与 invoke_logs |
| 鉴权缓存 + 默认拒绝 | `auth/AuthInterceptor.java` | 用户+权限码 30s TTL 缓存;**/api/** 无 @RequirePermission 注解一律 401**(原为默认放行),消除"忘加注解即裸奔" |
| 常量时间比较 | `auth/ServiceKeyInterceptor.java` | `MessageDigest.isEqual` |
| CORS 白名单 | `config/WebConfig.java` + `application.yml` | `gateway.cors.allowed-origins` 配置化,默认本地 6012/6013 |
| 健康检查 | 新增 `common/HealthController.java` | `GET /health`:DB 连接探测,不通 503 |
| 优雅停机 | `application.yml` | `server.shutdown: graceful` + 30s 排空 |
| 编译修复 | `pom.xml` | 显式 `maven-compiler-plugin` + Lombok annotationProcessorPaths + `<proc>full</proc>`——修复命令行编译时 Lombok getter 不生成的问题(IDE 编译不受影响) |

### 注意事项 / 遗留

1. **ai-service(Python)暂不透传 X-Request-Id**——NestJS 已发头,Java 已接收;需要 ai-service 把该头转发给 model-gateway 才能全链路打通(改 `ai-service` 的转发 headers,一行级改动,建议后续做)
2. Java 侧用户/权限缓存为 30s TTL(写操作都在 NestJS,该侧已有主动失效),用户禁用后 Java 侧最迟 30s 生效
3. `nest-cli.json` 的 `deleteOutDir` 已改为 false(构建直接覆盖 dist);`mvn` 命令行编译需 `JAVA_HOME=D:\Eclipse Adoptium\jdk-21.0.11` 且走 `mvn.cmd`
4. 未实施(见前文路线图):权限解析收敛为单一权威(R1)、Redis 化限流熔断(H1)、refresh token/登出吊销(S1)、Metrics 指标暴露(P2)
