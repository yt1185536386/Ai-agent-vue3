 怎么玩

  cd ai-service
  .venv/Scripts/python firstagent.py              # 交互模式,quit 退出
  .venv/Scripts/python firstagent.py 长沙天气     # 单次提问

  建议亲手做的两个小实验：

  1. 改 docstring 看行为变化：把 get_weather 的 docstring 删掉一半，再问天气，看模型还调不调——直观感受"docstring =
  提示词"
  2. 加一个自己的工具：比如 @tool def get_my_schedule() -> str: return "今晚 7
  点健身"，然后问"我今晚有什么安排"——体验"Agent 的能力 = 你给它什么工具"