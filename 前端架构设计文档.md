# 前端架构设计文档

> 范围:`packages/my-vue-app-ts`(业务对话端)、`packages/ServerManegeUI`(管理端)
> 版本基准:2026-09-10 优化后的现状(构建产物为实测数据)

---

## 1. 总体拓扑

```
┌──────────────┐         ┌──────────────────────┐
│ my-vue-app-ts │ :6012   │  ServerManegeUI :6013 │
│  业务对话端    │         │       管理端          │
└──────┬───────┘         └──────────┬───────────┘
       │ /api → 6011                │ /v1/pe,/v1/cx → 6010 (ai-service)
       │ (rewrite 去前缀)            │ /api,/v1 → 6015 (Java 网关)
       │                            │ /nestjs → 6011 (NestJS)
       ▼                            ▼
┌─────────────────────────────────────────────┐
│  NestJS BFF :6011  →  ai-service :6010      │
│                    →  model-gateway :6015   │
└─────────────────────────────────────────────┘
```

- **业务对话端**只跟 NestJS BFF 通信（`/api` 代理，rewrite 去掉 `/api` 前缀），不直连模型服务
- **管理端**是三个后端的编排入口：Prompt/Context 工程直连 ai-service，模型调用直连 Java 网关，模型管理/调用日志走 NestJS。注意 vite 代理按声明顺序匹配，`/v1/pe`、`/v1/cx` 必须写在 `/v1` 之前

## 2. my-vue-app-ts（业务对话端）

### 2.1 技术栈与构建

| 项 | 选型 |
|---|---|
| 框架 | Vue 3 + TypeScript + Vite 8（Rolldown） |
| 路由 | vue-router（history 模式） |
| 渲染依赖 | markdown-it + highlight.js(lib/core) + js-beautify |

**构建产物结构（manualChunks 函数形式）**：

```
dist/assets/
├── index-*.js          # 入口 + 路由骨架(首屏)
├── vendor-hljs.js      # highlight.js 按需注册产物(~63KB,变动少,长缓存)
├── vendor-markdown.js  # markdown-it + js-beautify(变动少,长缓存)
├── AgentMode-*.js      # 主界面(路由懒加载,含 mixin 核心逻辑)
└── Login-*.js          # 登录页(路由懒加载)
```

- **Vite 8/Rolldown 仅支持函数形式的 `manualChunks`**，对象形式会构建报错
- 分包按"变动频率"切分：vendor chunk 内容稳定，业务迭代不破坏浏览器长缓存
- **总体积 1.30MB → 454KB（-65%），gzip 后约 151KB**；首屏只需 index + 当前路由 chunk

### 2.2 路由架构

```ts
// 两个路由均为懒加载,登录页与主界面分包
{ path: "/",     component: () => import("../view/AgentMode/index.vue"), meta: { requiresAuth: true } }
{ path: "/login", component: () => import("../view/Login/index.vue"),   meta: { public: true } }
```

导航守卫链：`requiresAuth` 路由先跑 `loadAuth()`（auth 模块的本地凭证加载）再决定放行/跳登录，`public` 路由直通。

### 2.3 代码高亮按需注册

`mixin.ts` 中 highlight.js 从全量包改为 `highlight.js/lib/core` + 显式注册 10 种语言：

```ts
import hljs from "highlight.js/lib/core";
import javascript from "highlight.js/lib/languages/javascript";
// typescript / json / css / xml / python / java / bash / sql ...
hljs.registerLanguage("javascript", javascript);
```

- 主题样式仍用 `highlight.js/styles/vs.css`（浅色）
- **新增语言支持 = 加两行 import/register，不要改回全量包**
- 该项是本轮优化收益最大的一笔：全量包 190+ 语言压缩后约 900KB → 按需 63KB

### 2.4 核心数据流（SSE 对话）

```
index.vue ──POST /api/chat(completions)──▶ NestJS(BFF)
    ▲                                         │ 转发 ai-service
    └────── SSE 事件流(token/reasoning/error) ◀┘
```

- `AgentMode/index.vue` 持有消息列表（展示轨道），流式 token 逐个追加
- `mixin.ts` 承载对话核心逻辑（消息组装、regenerate、代码块处理），是当前最大的技术债（见 §5）

## 3. ServerManegeUI（管理端）

### 3.1 按需引入架构（Element Plus）

```ts
// vite.config.ts
AutoImport({ resolvers: [ElementPlusResolver()] }),   // ElMessage 等 API 自动导入
Components({ resolvers: [ElementPlusResolver()] }),   // <el-*> 模板组件自动注入
```

配套约定（**违反会出无声 bug**）：

1. `main.ts` 不再 `app.use(ElementPlus)`；中文语言包由 `App.vue` 的 `<el-config-provider>` 提供
2. **视图里显式 `import { ElMessage } from 'element-plus'` 的 JS 导入不走模板解析，样式必须手动引**——`main.ts` 里已补：
   ```ts
   import 'element-plus/es/components/message/style/css'
   import 'element-plus/es/components/message-box/style/css'
   ```
   今后新增用到的"纯 API 型"组件（notification / loading 等）同样要在这里补样式
3. 图标仍全量注册（体积占比小，换来了模板里直接用 `<el-icon>` 的自由度）

效果：主 chunk **1.30MB → 396KB（-70%）**，Element 组件拆成独立 chunk 随路由加载。

### 3.2 echarts 统一按需注册（`src/utils/echarts.ts`）

```ts
import * as echarts from 'echarts/core'
import { BarChart, LineChart } from 'echarts/charts'
// Grid / Tooltip / Legend + CanvasRenderer → echarts.use([...])
export type EChartsOption = ComposeOption<...>   // 类型也与全量包等价
export type ECharts = ReturnType<typeof echarts.init>
export default echarts
```

- **铁律：视图一律 `import echarts from '@/utils/echarts'`，禁止 `import * as echarts from 'echarts'`**
- 新增图表类型（如 PieChart）= 在该文件 `echarts.use` 与类型组合里各加一行
- 效果：echarts 产物 **1016KB → 516KB（-49%）**，Dashboard / ContextMetrics / PromptMetrics 三视图共用

### 3.3 视图结构

```
src/views/
├── Dashboard/        # 概览(echarts)
├── ContextMetrics/   # 上下文工程指标(echarts)
├── PromptMetrics/    # Prompt 工程指标(echarts)
└── ...(模型管理 / 用户权限 / 调用日志等,走 NestJS 与 Java 网关)
```

路由层已全部懒加载（优化前即是），配合组件按需引入，各页面 chunk 独立。

## 4. 安全与运维约定

| 项 | 约定 |
|---|---|
| CORS | 由后端白名单控制（NestJS `CORS_ORIGINS` / Java `gateway.cors.allowed-origins`），前端开发端口 6012/6013 已在默认白名单 |
| 凭证 | 登录 token 存本地（auth 模块），随请求头携带；过期由后端 401 驱动跳登录 |
| 环境变量 | `VITE_PORT`、`NESTJS_PORT`（业务端）；管理端端口固定 6013 |

## 5. 性能预算与遗留债务

**已达标**：两端口 JS 总体积分别 454KB / 主 chunk 396KB，首屏（路由级）加载 ≤ ~200KB。

**遗留（按优先级）**：

| 债务 | 位置 | 影响 | 建议 |
|---|---|---|---|
| 巨型组件 | `AgentMode/index.vue` 约 2800 行 | 可维护性 | 拆分为消息列表 / 输入区 / 设置面板子组件（单独立项） |
| `@ts-nocheck` | `mixin.ts` 约 1100 行 | 核心逻辑无类型检查 | 拆分时逐段补类型 |
| 流式渲染 deep watch | `index.vue` 对 `activeMessages` 深度监听 | 每个 token 触发 `querySelectorAll`+`offsetTop` 强制回流，长消息打字卡顿 | 流式期间跳过导航重算，或虚拟滚动 |
| `:key="index"` | 消息列表 v-for | regenerate splice 截断时复用错位 | 改用消息稳定 id |
| 未压缩大图 | icon1.png 476KB / image_87x.png 700KB | 首次加载体积 | 转 WebP（需确认清晰度要求） |
